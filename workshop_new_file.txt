1. branch
    a. git checkout -b new_branch_name (creates and switches/checks out).
    b. git push origin new_branch_name (pushes to that branch. don't push to master, it's safer).
    c. < 3 people working per branch to minim. conflicts & breakage and reverts impacting too many other folks code. 
    d.
2. merge
3. clone
4. checkout
